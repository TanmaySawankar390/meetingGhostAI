port: 7880
room:
    empty_timeout: 300
    departure_timeout: 60
rtc:
    tcp_port: 7881
    port_range_start: 50000
    port_range_end: 60000
    # In production on EC2, LiveKit MUST use the external IP so browsers can bounce ICE candidates correctly
    use_external_ip: true
keys:
    devkey: livekit_super_secret_production_key_12345
logging:
    json: false
    level: info
